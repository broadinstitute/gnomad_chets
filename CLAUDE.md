# gnomad_chets — Project Instructions

## Conda environment

Always use the `gnomad_chets` conda env in this workspace (cloned from `hail`).
This **overrides** the global `hail` env preference in `~/.claude/CLAUDE.md` for this workspace only.

Activate with:
```
source /Users/jgoodric/miniconda3/etc/profile.d/conda.sh && conda activate gnomad_chets
```
